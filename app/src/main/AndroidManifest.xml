<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pc_render.mobpdi">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:name=".config.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/oneicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/oneicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <activity android:name=".Pin"
            android:windowSoftInputMode="stateAlwaysHidden"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".IDadmob"
            android:noHistory="true"/>
        <activity android:name=".vpn.Vpn"
            android:noHistory="true"/>
        <activity android:name=".Info"
            android:noHistory="true"/>

        <activity android:name=".gilinguang.SatuIklan" android:noHistory="true"/>
        <activity android:name=".Menu" android:noHistory="true"/>
        <activity android:name=".gilinguang.SatuIklanInterest" android:noHistory="true"/>
        <activity
            android:name="com.google.android.gms.ads.AdActivity"
            android:noHistory="true"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize" />


    </application>

</manifest>